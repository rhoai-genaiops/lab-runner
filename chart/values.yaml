image:
  repository: quay.io/rhoai-genaiops/lab-runner
  tag: 0.0.1
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 8080

route:
  enabled: true

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 512Mi
